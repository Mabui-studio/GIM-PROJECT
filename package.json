{
  "name": "betta-master-2026",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:browser": "esbuild src/browser/game.ts --bundle --outfile=public/game.js --format=iife --platform=browser",
    "test": "node --experimental-vm-modules node_modules/vitest/vitest.mjs run"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.6"
  }
}
